- Operators:
    Control: Screen
    Properties:
      OnVisible: |-
        =Reset(operator_1);
        Reset(operator_2);
        Reset(operator_3);
        Reset(operator_4);
        Reset(operator_5);

        UpdateContext({locPadding: Self.Width * 0.005});