- Materials:
    Control: Screen
    Properties:
      OnVisible: |-
        =UpdateContext({
                locPadding: Self.Width * 0.005,
                locError: false
                });

            Select(m_update);