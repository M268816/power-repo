- Downtime_Screen:
    Control: Screen
    Properties:
      OnVisible: |-
        =/*Init Local Variables*/
        UpdateContext({locShowTop: true});

        /*Init Time Variables*/
        Set(varNowDay, Today());
        Set(varNowHour, Hour(Now()));

        /*Call the update button*/
        Select(d_collect);