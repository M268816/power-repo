Properties:
  OnVisible: |-
    =/*Init Local Variables*/
    UpdateContext({
        locPopup: false,
        locContent: Blank()
        });

    /*Init time variables*/
    Set(varNowDay, Today());
    Set(varNowHour, Now());

    /*Refresh universal variables so they update in the list correctly.*/
    Refresh(Universal_Variables);